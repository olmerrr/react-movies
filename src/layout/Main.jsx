import './Main.css';

export function Main() {
  return <main className="container content">
   <div>
   this is main cmp
    this is main cmp
    this is main cmp
    this is main cmp
   </div>
  </main>
}